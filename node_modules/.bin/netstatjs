#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../netstat/bin/netstatjs" "$@"
  ret=$?
else 
  node  "$basedir/../netstat/bin/netstatjs" "$@"
  ret=$?
fi
exit $ret
